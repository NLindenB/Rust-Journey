// A `Mismatch Error` Example
// fn main() {

//     let x: u8 = 5;
//     let y: i32 = -128;
//     let z: i32 = x + y;

//     println!("Arithmetic in Rust can be executed when the values have the same data type, for example:");
//     println!("5 + (-128) = {}", z);
// }

fn main() {

    let x: i32 = 5;
    let y: i32 = -128;
    let z: i32 = x + y;

    println!("Arithmetic in Rust can be executed when the values have the same data type, for example:");
    println!("5 + (-128) = {}", z);
}